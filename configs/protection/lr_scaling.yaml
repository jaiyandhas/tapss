protection:
  policy: "lr_scaling"
  # LR multiplier = 1 - (importance * scale_factor)
  # So high-importance params get lower effective LR
  scale_factor: 0.9
  min_lr_ratio: 0.05     # Never go below 5% of base LR
